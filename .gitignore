# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# Temporary files generated by your text editor or operating system
# belong in git's global ignore instead:
# `$XDG_CONFIG_HOME/git/ignore` or `~/.config/git/ignore`

# === SENSITIVE INFORMATION ===
# Environment files with API keys and secrets
/.env*
!/.env.example

# Rails master key and credentials
/config/master.key
/config/credentials.yml.enc

# Database configuration with passwords
/config/database.yml
!/config/database.yml.example

# Google Cloud credentials and keys
/gcp-key.json
/service-account-key.json
/.config/gcloud/
/google-cloud-key.json
*-key.json
*-credentials.json

# Cloud Run and GCP deployment files with sensitive data
/app.yaml.production
/cloudbuild.yaml.production

# === GEM DEVELOPMENT ===
# Bundler
/.bundle/
/vendor/bundle/

# Built gems
/*.gem

# RubyGems credentials
/.gem/

# === RAILS API ===
# Logfiles and tempfiles
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep

# Pidfiles
/tmp/pids/*
!/tmp/pids/
!/tmp/pids/.keep

# Storage (uploaded files and SQLite databases)
/storage/*
!/storage/.keep
/tmp/storage/*
!/tmp/storage/
!/tmp/storage/.keep

# === TESTING ===
# Test coverage reports
/coverage/

# RSpec status file
/.rspec_status

# VCR cassettes (may contain sensitive API responses)
/spec/vcr_cassettes/

# === DEVELOPMENT TOOLS ===
# IDE and editor files
/.vscode/
/.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# === DEPLOYMENT ===
# Docker production files
/Dockerfile.prod
/docker-compose.prod.yml
/docker-compose.override.yml

# Deployment scripts with sensitive info
/deploy/
/scripts/deploy*.local
/scripts/*-prod.sh

# Cloud deployment configurations
/terraform/
/ansible/
/.kube/
/k8s/secrets/

# Cloud provider specific
# AWS
/.aws/
/aws-credentials
# Azure
/.azure/
# Google Cloud (additional patterns)
/.gcloud/
/gcp-config/

# === DOCUMENTATION ===
# Generated documentation
/doc/
/.yardoc/

# === BACKUP FILES ===
*.bak
*.backup
*.old

# === CLOUD RUN / GCP SPECIFIC ===
# Build artifacts
/build/
/.build/

# Cloud Run service files with project-specific data
/app.yaml.local
/service.yaml

# GCP project configuration
/.gcp/
/gcp-project-config.json

# Cloud SQL proxy
/cloud_sql_proxy

# === MONITORING AND LOGS ===
# Application logs
/logs/
*.log.*
/log/*.log

# Monitoring data
/metrics/
/traces/

# === TEMPORARY BUILD FILES ===
# Ruby/Rails temporary files
/.bundle/
/vendor/bundle/
/tmp/cache/
/tmp/pids/
/tmp/sessions/
/tmp/sockets/

# Node.js (if using for assets)
/node_modules/
/npm-debug.log*
/yarn-debug.log*
/yarn-error.log*

# === SECURITY SCANS ===
# Security scan results
/security-scan-results/
/vulnerability-reports/
*.security-report
